@page
@model RazorPages.Pages.AddItemModel
@{
}
@(Html.DevExtreme().Form<DataModels.PhoneBook>()
    .Items(items => {
        items.AddGroup().Caption("Group1").Items(groupItems => {

            groupItems.AddSimpleFor(m => m.IdPb);

            groupItems.AddSimpleFor(m => m.Name);

            groupItems.AddSimpleFor(m => m.Patronymic);

            groupItems.AddSimpleFor(m => m.Surname);

            groupItems.AddSimpleFor(m => m.Phone);

            groupItems.AddSimpleFor(m => m.Sex);
        });
    })
)